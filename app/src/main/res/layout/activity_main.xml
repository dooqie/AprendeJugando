<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/fondo_menu_v2"
    android:orientation="vertical"
    android:padding="16dp">

    <!-- Header con usuario (fijo en la parte superior) -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:padding="12dp"
        android:background="@android:color/transparent"
        android:elevation="0dp"
        android:layout_marginBottom="16dp">

        <TextView
            android:id="@+id/tvBienvenida"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="¡Hola!"
            android:textColor="#FFFFFF"
            android:textSize="32sp"
            android:fontFamily="@font/nunito_extrabold"
            android:textStyle="normal" />

        <androidx.appcompat.widget.AppCompatButton
            android:id="@+id/btnCerrarSesion"
            android:layout_width="100dp"
            android:layout_height="50dp"
            android:text="Cerrar"
            android:onClick="onCerrarSesionClicked"
            android:background="@drawable/btn_action_red"
            android:textColor="@color/text_light"
            android:textSize="16sp"
            android:fontFamily="@font/nunito_semibold"
            android:textStyle="normal"
            android:textAllCaps="false" />
    </LinearLayout>

    <!-- Grid de módulos dentro de un ScrollView para que entren todos los tiles -->
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:fillViewport="true"
        android:scrollbars="vertical"
        android:overScrollMode="ifContentScrolls">

        <GridLayout
            android:id="@+id/gridModulos"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:columnCount="2"
            android:padding="8dp" />

    </ScrollView>

</LinearLayout>
